Pin A Bug README

As of latest commit:
- User can create an account in the app (firebase API)
- User can log in through the app (firebase API)
- User can create posts that are stored as JSON file (offline)
- User can see map
- User can see their location on map (online)
- User can see pins on map of where posts were uploaded from
- User can toggle different themes in settings
- User can erase post history in settings
